server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/identity_service"
    username: "root"
    password: "root"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jwt:
    secret: ${SPRING_JWT_SECRET}
    refresh_secret: ${SPRING_JWT_REFRESH_SECRET}
